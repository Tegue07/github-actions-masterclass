name: "01 Hello World!"

on:
  workflow_dispatch: #This event lets you trigger the workflow manually

jobs:
  say_hello_job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Hello World
        run: echo "Hello, World!"